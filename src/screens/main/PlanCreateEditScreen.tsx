import { View } from 'react-native';
import { Typography } from '../../components';

interface PlanCreateEditScreenProps {}

export const PlanCreateEditScreen = (props: PlanCreateEditScreenProps) => {
  return (
    <View>
      <Typography variant="heading">Create/Edit Plan</Typography>
    </View>
  );
};
